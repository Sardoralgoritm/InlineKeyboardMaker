namespace InlineKeyboardBot.Models.Dto.Enums;

public enum ButtonLayoutType
{
    SingleColumn = 0,      // Har biri alohida qatorda
    TwoColumns = 1,        // Ikkitadan bir qatorda
    ThreeColumns = 2,      // Uchta bir qatorda
    AllInOneRow = 3,       // Barchasini bir qatorda
    Custom = 4             // Maxsus tartib
}